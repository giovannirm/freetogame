import { ImageSuffixPipe } from './image-suffix.pipe';

describe('ImageSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
